type Txn {
    txnHash: String
    blockNumber: Int
    topics: [String] 
}


type Query {

    getTokenName(address: String): String!

    getTotalSupply(address: String): String!

    getTxnsList(address: String, block: Int): [Txn]

    getPriceETH(address: String): String!

    getPriceUSD(address: String): String!

    
}